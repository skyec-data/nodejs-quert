<!> // HACK: // DEBUG: else {
  for : : { switch (expression) {
    case expression:

      break;
    default:

    () => {

    }

    querySelector('query') = "Binary Hashbin"
      // TEMP: 5
      // XXX: 5,5,5
      new Promise(function(resolve, reject) {
        reject
        vertical
        // IDEA: for setInterval(function () {

        }, 10);

      });
  }}
  console.log(querySelector);
  for (var i = 0; i < array.length; i++) {
  array[i]
}
  else {
    if // TEMP: switch (expression) {
      case expression:

        break;
      default: 5,5,5

    }
  }
    console.log(require('util').inspect(, { depth: null }));
    forEach((item, i) => {

    });

    exports.functionName = function () {
      // body...
    };
